<div class="modal fade" id="modalConfirmDeleteFinancialYearCompanyRelation" role="dialog" data-backdrop="static" data-keyboard="true" aria-hidden="true">
    <div class="modal-dialog modal-md">
        <div class="modal-content">
            <div class="modal-header">
                <div class="modal-title classh4">Confirm Delete</div>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span class="modal-header-close" aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete?</p>
            </div>
            <div class="modal-footer justify-content-between">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" id="btn-delete-financialYearCompanyRelation">Delete</button>
            </div>
        </div>
    </div>
</div>

<script>
    // remove element button click.
    var _financialYearCompanyRelationId = null;
    function fnDeleteFinancialYearCompanyRelation(element, financialYearCompanyRelationId) {
        _financialYearCompanyRelationId = financialYearCompanyRelationId;
        $(element).attr("data-target", "#modalConfirmDeleteFinancialYearCompanyRelation");

        $('#modalConfirmDeleteFinancialYearCompanyRelation').on('show.bs.modal', function (e) {
            // Pass form reference to modal for submission on yes/ok
            var form = $(e.relatedTarget).closest('form');
            $(this).find('.modal-footer #modalConfirmDeleteFinancialYearCompanyRelation').data('form', form);
        });
    }

    // form confirm (yes/ok) handler, submits form
    $('#modalConfirmDeleteFinancialYearCompanyRelation').find('.modal-footer #btn-delete-financialYearCompanyRelation').on('click', function () {
        var model = { financialYearCompanyRelationId: _financialYearCompanyRelationId };
        AjaxCommunication.CreateRequest(this.window, 'Post', '@Url.Action("DeleteFinancialYearCompanyRelation", "FinancialYearCompanyRelation")', '', model,
        function (data) {
            if (data.Result.Status) {
                $('#modalConfirmDeleteFinancialYearCompanyRelation').modal('hide');
                $('#financialYearCompanyRelationGrid').DataTable().ajax.reload();
                fnSuccessNotify('Record deleted.');
            }
            else {
                fnDangerNotify('Error occured while processing request.');
            }
        }, null, true, null, false);
    });

</script>
